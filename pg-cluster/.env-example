# PostgreSQL Credentials (for the superuser)
POSTGRES_USER=postgres_admin
POSTGRES_PASSWORD=******
# CORRECTED: The database name the app expects
POSTGRES_DB=trading_db

# Application User Credentials (for the trading-app)
APP_USER=trading_app_user
APP_USER_PASSWORD=******

# Postgres Exporter Configuration
# CORRECTED: Point to the correct database name
DATA_SOURCE_NAME=postgresql://postgres_admin:${POSTGRES_PASSWORD}@db:5432/trading_db?sslmode=disable