#--- START OF FILE /opt/pg-cluster/.env ---
# --- Environment variables for the database stack ---

# PostgreSQL Credentials (for the superuser)
# IMPORTANT: Replace with a strong, randomly generated password.
POSTGRES_USER=postgres_admin
POSTGRES_PASSWORD=YOUR_STRONG_SUPERUSER_PASSWORD_HERE
POSTGRES_DB=trading_db

# Application User Credentials (for the trading-app)

# Application User Credentials
APP_USER=trading_app_user

# IMPORTANT: Replace with a different, strong, randomly generated password.
APP_USER_PASSWORD=YOUR_STRONG_APP_USER_PASSWORD_HERE

# OR

# SSA-1: The password is now read from a file, not stored here.
APP_USER_PASSWORD_FILE=/opt/secrets/trading_app_password.txt

# Postgres Exporter Configuration
# This uses the superuser to gather comprehensive metrics.
# The password is automatically passed by Docker Compose.
DATA_SOURCE_NAME=postgresql://postgres_admin:${POSTGRES_PASSWORD}@db:5432/trading_db?sslmode=disable
#--- END OF FILE ---
